import stdio::*

fn optional_result(x i32) ?i32 :
    if x < 0i32:
        Some[i32][x]
    else:
        Null[i32][]

fn main():
    mut x = optional_result(-32)
    // when x == 32: --> Null
    // for both cases Some or  Null
    // Error 1042: No matching method '<-' found that matches the call's arguments.
    // -->     print <- x, "\n"
    // print <- x, "\n"