// Macro
import stdio::*

macro max[x, y]:
  imm xx = x
  imm yy = y
  if xx > yy {xx} else {yy}

fn test(i i32, j i32):
  imm maxnbr = max(i, j)
  print <- maxnbr, "\n"  // => 5

fn main():
  test(4, 5)
