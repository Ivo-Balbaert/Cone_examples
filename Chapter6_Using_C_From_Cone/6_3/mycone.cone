import stdio::*

extern:
    fn my_add_int(a i32, b i32) i32
    fn my_add_double(a f64, b f64) f64

fn main():
    print <- "\n", my_add_int(3,8), "\n"              // => 11
    print <- "\n", my_add_double(38.3d,83.5d), "\n"   // => 121.8