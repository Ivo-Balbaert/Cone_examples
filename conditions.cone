import stdio::*

fn main():
  print <- abs(7), "\n"   // => 7
  print <- abs(-7), "\n"  // => 7

  /* mut a = 0
  // if not("hello" == ""): a += 1 // doesn't work
  if 42: a += 1
  print <- a, "\n"  // => 1
  */

fn abs(x i32) i32:
  if x < 0:
    -x
  elif x == 0:
    0
  else:
    x